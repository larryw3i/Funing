# Funing
```python
install_requires=[
    'opencv-python',
    'langcodes[data]',
    'pony',
    'pyyaml',
]
```
a **simple** face recognition GUI, try it just do:  
```bash
pip3 install funing; funing
```